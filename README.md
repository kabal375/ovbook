# ovbook

CLI tool that converts books (fb2/epub/pdf) into structured markdown chunk trees for OpenViking indexing.

## Usage

```bash
ovbook convert book.fb2 --output ~/ov-lib/tech-lib/
```

## Install

```bash
uv tool install .
# or
pipx install .
```
